# Keep the build context small and deterministic. The Dockerfile copies only
# src/, the frozen replay assets and the gateway entrypoint; everything else is
# upload cost and noise.
.git/
.venv/
**/__pycache__/
**/*.pyc
.ruff_cache/
.pytest_cache/
web/node_modules/
