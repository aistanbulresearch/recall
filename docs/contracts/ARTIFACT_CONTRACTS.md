# Recall Artifact Contracts

- Status: corrected design baseline; implementation not started
- Date: 2026-08-17
- Related tasks: RCL-202, RCL-209, RCL-210
- Correction authority: ADR-0008

## Normative rules

1. Every artifact is strict, immutable, versioned, and rejects unknown fields recursively.
2. Missing required data is an error, not an empty collection, `false`, `0`, or a clean result.
3. Contract versions use semantic version strings. A breaking field or meaning change increments the major version.
4. Producers emit canonical UTF-8 JSON. Content hashes use SHA-256 over canonical JSON with `content_hash` omitted.
5. Artifact IDs are opaque UUIDs. Time values are UTC RFC 3339 strings.
6. All enums reject unknown values. No producer may invent a fallback enum.
7. Models can propose only contracts assigned to their role. The Ledger API validates producer identity and artifact type.
8. `warnings` never convert a failed prerequisite into success.
9. Sensitive raw text, direct identifiers, token maps, secrets, prompts, and chain-of-thought are forbidden in every cloud artifact.

## Common envelope version 1.0.0

| Field | Type | Required | Constraint |
|---|---|---|---|
| `schema_name` | string | yes | Exact registered contract name |
| `schema_version` | string | yes | Exact supported semantic version |
| `artifact_id` | UUID string | yes | Globally unique and immutable |
| `case_id` | UUID string or null | yes | Null only for deployment-level receipts |
| `run_id` | UUID string or null | yes | Required for run-scoped artifacts |
| `producer.component` | string | yes | Registered producer name |
| `producer.version` | string | yes | Exact deployed build or policy version |
| `producer.identity` | string | yes | Sanitized service identity alias, not an email or secret |
| `created_at` | UTC timestamp | yes | Generated by trusted runtime clock |
| `input_artifact_ids` | array of UUID strings | yes | Sorted, unique, may be empty only for roots |
| `content_hash` | lowercase hex string | yes | SHA-256 canonical-content hash |
| `data_mode` | enum | yes | `SYNTHETIC`, `CAPTURED_REPLAY`, `LIVE_PUBLIC`, or `MOCK` |
| `status` | enum | yes | `VALID`, `INVALID`, `INCOMPLETE`, `REJECTED`, or `DEGRADED` |
| `warnings` | array of typed warning objects | yes | Empty array is explicit, not omitted |
| `extensions` | object | yes | Empty by default; only registered namespaced keys allowed |

Each warning has exactly `code`, `message_key`, and `related_artifact_ids`. Free-form sensitive text is prohibited.

## Corrected payload contract versions

The common envelope remains version `1.0.0`. ADR-0008 changes several payload meanings before implementation, so their contract versions follow the major-version rule instead of silently retaining the earlier draft version:

| Contract | Version | Reason |
|---|---|---|
| `CandidateDeltaReceipt` | `1.0.0` | New deterministic candidate-routing artifact |
| `WatchCase` | `2.0.0` | Adds pending observations and typed attention state |
| `EvidenceDelta` | `2.0.0` | Requires deterministic candidate receipt linkage |
| `DataModeReceipt` | `2.0.0` | Replaces scalar mode fields with mode set and composition |
| `ScanRun` | `1.1.0` | Adds the closed `execution_profile`; `1.0.0` remains a read-only legacy contract |
| `PrivacyReceipt` | `1.1.0` | Adds model execution locus, transport, endpoint, model identity, and immutable model revision; `1.0.0` remains a read-only legacy contract |
| `AgentExecutionReceipt` | `1.0.0` | Records one STARTED or terminal role attempt with bounded ADK/model/tool telemetry |
| `CohortDayManifest` | `3.3.0` | Adds durable cycle-attempt recovery, batch-receipt parity, and explicit trigger/write/agent/end-to-end deadlines; strict versions through `3.2.0` remain read-only legacy contracts |
| `BatchExecutionReceipt` | `1.0.0` | Freezes one cycle-attempt's exact run set and measured fresh-versus-recovered write classification before agent execution |
| `CohortExecutionCheckpoint` | `1.0.0` | Preserves completed per-case outcomes plus typed failed cases when cohort execution cannot produce a manifest |
| `CohortDayFailureReceipt` | `1.0.0` | Deterministic record of a missing prior-day manifest after zero-run/event reconciliation |
| `CohortHistoryReceipt` | `1.0.0` | Immutable bridge from the committed Day-1 run evidence into Day-N history |
| `CompressedCycleFailureReceipt` | `1.0.0` | Explicit owner-reported record of the failed legacy Day-2 attempt carried into compressed recovery history |
| `CohortHeadroomReceipt` | `1.0.0` | Content-addressed c1-c5 evidence snapshot and deterministic c6 admission decision |
| `CohortRampGateReceipt` | `1.0.0` | Content-addressed predecessor integrity, parity, and end-to-end performance gate for plan-5 ramp cycles |
| `PolicyDecision` | `2.0.0` | Replaces Boolean facts with evaluated-state enums and removes memory fact |

All unchanged payload contracts remain at `1.0.0` until an executable schema records a later compatible revision.

## Contract catalog

| Contract | Required payload fields | Authoritative producer | Consumers | Forbidden content or authority |
|---|---|---|---|---|
| `PrivacyReceipt` | Version `1.1.0`; legacy fields plus `execution_locus`, `transport_class`, `endpoint_class`, `model_id`, `model_revision` | Local Privacy Gate | Intake, UI | Raw/redacted values, token map, proof that data is clinically anonymous, unbound execution-location claims |
| `WatchCase` | `tenant_id`, `region`, `state`, `monitoring_started_at`, `monitoring_policy`, `next_scan_at`, `source_cursors`, `last_verified_snapshot_id`, `last_verified_scan`, `pending_observation_hashes`, `attention_marker`, `open_review_task_id`, `retention_policy` | Controller through Ledger | Scheduler, Controller, UI | Identity, model prose, clinical classification |
| `ScanRun` | `watch_case_id`, `state`, `scheduled_for`, `attempt`, `lease_epoch`, `deadline_at`, `budget_snapshot`, `idempotency_key`, `trace_id`, `terminal_policy_decision_id`, `failure_receipt_ids` | Controller through Ledger | Controller, Policy, UI | Agent-written transition, hidden retries |
| `ScanRunEvent` | `event_id`, `sequence`, `from_state`, `to_state`, `event_code`, `agent_id`, `lease_epoch` | Controller through Ledger | Controller, UI, evaluation | Model-authored state transition |
| `RoutingPlan` | `requested_capabilities`, `proposed_bindings`, `route_order`, `validation_status`, `rationale_codes` | Fleet Coordinator | Controller | Endpoint credentials, arbitrary URL, direct invocation command, outcome |
| `RegistryResolutionReceipt` | Version `1.1.0`; `requested_capabilities`, `bindings`, `resolution_mode`, `validation_status`, `reason_codes` | Controller | Controller, UI, Policy completeness | Unvalidated endpoint as approved |
| `ToolAuthorizationReceipt` | `agent_role`, `tool_id`, `requested_action`, `decision`, `policy_version`, `reason_codes`, `invocation_id` | Gateway or Controller authorizer | Controller, Auditor, UI | Secret token, alternate credential instructions |
| `EvidenceObservation` | `source`, `source_record_id`, `retrieved_at`, `source_version`, `source_locator`, `source_content_hash`, `structured_fields`, `retrieval_status` | Evidence connector | Watcher, Ledger | Raw hostile instructions as executable text, unsupported interpretation |
| `EvidenceSnapshot` | `effective_at`, `observation_ids`, `coverage_status`, `source_cursors`, `normalized_facts`, `conflicts`, `snapshot_hash` | Evidence Watcher | Assessor, Policy completeness, UI | Classification or review recommendation |
| `CandidateDeltaReceipt` | `previous_snapshot_id`, `current_snapshot_id`, `exact_allele_match`, `scope_match`, `snapshot_complete`, `new_observation_hashes`, `candidate_delta_state`, `reason_codes` | Deterministic Evidence Normalizer | Controller, Assessor, Policy, UI | Agent proposal, clinical interpretation, model materiality decision |
| `EvidenceDelta` | `candidate_receipt_id`, `previous_snapshot_id`, `current_snapshot_id`, `added_observation_refs`, `removed_observation_refs`, `change_items`, `comparison`, `materiality_proposal`, `uncertainties`, `counter_evidence_refs` | Evidence Assessor | Auditor, Policy, UI | Final clinical class, unreferenced claim, candidate-path suppression |
| `AssessmentReceipt` | `delta_id`, `material_claims`, `counter_evidence_set`, `uncertainty_codes`, `schema_validation_status` | Evidence Assessor | Auditor | Policy outcome, task request |
| `CitationAuditReceipt` | `assessment_id`, `audit_status`, `claim_verdicts`, `metadata_refetches`, `counter_evidence_coverage`, `audit_completeness`, `rejected_claim_ids` | Citation Auditor | Policy, UI | Trust based solely on assessor text, task creation |
| `MemoryAdmissionReceipt` | `memory_candidate_hash`, `scope`, `topic`, `source_refs`, `expires_at`, `decision`, `reason_codes` | MemoryAdmissionGate | Memory adapter, UI | Identity, classification, policy outcome, unsupported evidence |
| `MemoryRetrievalReceipt` | `query_hash`, `scope`, `returned_memory_refs`, `expiry_check`, `contradiction_check`, `decision`, `reason_codes` | Memory retrieval gate | Agent adapter, UI | Memory body in telemetry, evidence-complete assertion |
| `DataModeReceipt` | `subject_artifact_ids`, `mode_set`, `declared_composition`, `propagation_status`, `reason_codes` | Deterministic mode gate | API, UI, Policy completeness, release audit | Silent mode conversion, scalar trust ordering |
| `ScanRun` | Version `1.1.0`; legacy fields plus `execution_profile` (`CREATE_ONLY` or `FULL_AUDIT_V1`) | Controller through Ledger | Ledger, scheduler, operations evidence | A created work item being presented as agent-executed work |
| `AgentExecutionReceipt` | `execution_profile`, `agent_role`, `attempt`, `execution_status`, runtime/model/endpoint/trace identity, start/completion/latency, per-turn token and finish telemetry, `http_429_count`, tool call/response IDs, causal `tool_records`, STARTED binding, typed failure | Controller agent executor | Ledger, manifest exporter, UI, audit | Agent Engine misattribution, missing attempt provenance, unbound tool claims, hidden timeout/rate-limit/schema failure, chain-of-thought storage |
| `CohortDayManifest` | Version `3.3.0`; `3.2.0` fields plus `cycle_attempt_id`, `batch_execution_receipt_id`, `write_measurement_status`, strengthened parity, and `deadline_policy`; post-parse verification binds outcomes and receipts to ledger artifacts and the hash-verified plan | Cohort scheduler | UI cohort panel, operations evidence | Wall-clock substitution, source/image/plan drift, warm-cache shortcut, recovery misreported as fresh work, aggregate-only agent claims, Agent Engine misattribution |
| `BatchExecutionReceipt` | `plan_sha256`, `cycle_id`, `cycle_attempt_id`, ordered authoritative run/artifact IDs, fresh-created and recovered-current-epoch IDs, `measurement_status`, exact write metrics | Cohort scheduler | Manifest producer, successor gates, audit | Re-measured recovery, cross-epoch reuse, inferred fresh-write parity |
| `CohortExecutionCheckpoint` | Plan/cycle/expected-manifest identity, completed outcomes, failed cases, total count, `checkpoint_status`, `policy_outcomes_synthesized=false`; every completed row is reconciled against its terminal pointer and the verified immutable ScanRun, PolicyDecision/CitationAudit/Failure, and paired AgentExecutionReceipt dependencies are retained in `input_artifact_ids` | Cohort scheduler | Recovery, operations, audit | Fabricated PolicyDecision, transient-only pointer checks, discarded completed cases, free-form exception body |
| `CohortDayFailureReceipt` | `day_index`, `selected_for_date`, `detected_at`, `failure_code`, `expected_manifest_id`, `runs_predicted`, `runs_created`, `source_commit`, `image_digest`, `continuation_policy` | Cohort scheduler | Cohort scheduler, UI cohort panel, operations evidence | Missing-day concealment, fabricated zero-run claims, retry timestamp drift |
| `CohortHistoryReceipt` | `evidence_path`, `evidence_sha256`, `evidence_git_blob_oid`, `source_commit`, `source_tree`, `phase`, `trigger_code`, `day_index`, `executed_at`, `selected_for_date`, `created_run_ids`, `selected_case_ids`, `excluded_case_ids`, `runs_created`, `runs_predicted`, `readback_counts`, `direct_exit_code`, `evidence_classification`, `atomic_check_ids` | Cohort history loader | Cohort scheduler, audit | Free historical literals, second-run timestamp substitution, mutable worktree evidence |
| `CompressedCycleFailureReceipt` | `cohort_due_date`, `scheduled_for`, `failure_code`, `runs_predicted`, `runs_created`, `evidence_state`, `decision_reference`, `continuation_policy` | Cohort scheduler | Cohort scheduler, UI cohort panel, audit | Mechanism-proved overstatement, invented execution time, hidden failed attempt |
| `CohortHeadroomReceipt` | `plan_sha256`, `input_snapshot_sha256`, `gate_version`, `required_cycle_ids`, ordered `observed_cycles` with both manifest-derived `runs_created` and ledger `scan_runs_readback`, aggregate run/event counts, `decision`, `reason_codes`, `evidence_watermark` | Cohort scheduler | c6 scheduler, UI, audit | Mutable gate decision, environment override, count substitution, unbound c1-c5 evidence |
| `CohortRampGateReceipt` | `target_plan_sha256`, `target_cycle_id`, `input_snapshot_sha256`, `gate_version`, `metric_policy`, exact `predecessor_binding`, `observed_metrics`, `decision`, `reason_codes`, `evidence_watermark` | Cohort scheduler | plan-5 scheduler, UI, audit | Mutable gate decision, inferred predecessor, unbound evidence, reuse hidden as new work, non-live performance claim |
| `PolicyDecision` | `policy_version`, `input_facts`, `outcome`, `reason_codes`, `missing_prerequisites`, `review_trigger`, `existing_task_id` | Deterministic Policy Gate | Controller, Ledger, UI | Model prose as input, notification side effect |
| `ReviewTask` | `watch_case_id`, `trigger_decision_id`, `state`, `priority_band`, `claim_ids`, `audit_receipt_id`, `simulation`, `deduplication_key` | Controller transactional outbox | Reviewer UI | Patient contact, autonomous clinical action, unlabeled real task |
| `HumanDecisionReceipt` | `review_task_id`, `actor_role`, `action`, `reason_code`, `decided_at` | Authenticated reviewer workflow | Controller, Ledger | Agent identity as reviewer, free-text patient content |
| `FailureReceipt` | `failure_code`, `stage`, `retryable`, `attempt`, `budget_state`, `details`, `related_artifact_ids`, `safe_terminal`, `operator_action` | Deterministic component detecting failure | Controller, Policy, UI, operations | Fabricated policy outcome, sensitive stack/body data |
| `DeploymentReceipt` | `runtime`, `deployed_components`, `source_revision`, `deployed_at` | Release controller | UI, audit | Decorative or unverified cloud claim |
| `ManagedPathReceipt` | `managed_status`, `component_statuses`, `reason_codes`, `trace_id` | Deterministic health aggregator | UI, operations | Default healthy state |
| `HistoricalReplayEvaluation` | `protocol_version`, `candidate_id`, `lead_time_days`, `positive_detected`, `negative_control_results`, `manifest_ref` | Evaluation harness | UI, claim ledger | Generalized clinical claim |
| `UtilityEvaluation` | `protocol_version`, `case_count`, `review_task_count`, `manifest_ref` | Evaluation harness | UI, claim ledger | Hidden denominator or extrapolation |
| `PrivacyEvaluation` | `protocol_version`, `test_count`, `gemma_incremental_true_positive_count`, `accepted_escape_count`, `manifest_ref` | Evaluation harness | UI, claim ledger | Real-data or clinical privacy claim |

## Normative nested value shapes

The catalog field names above are not open objects. These nested structures are also strict and reject unknown fields:

| Path | Required shape |
|---|---|
| `PrivacyReceipt.detectors.deterministic` | `version`, `approved_spans` as an array of keyed span-hash string references; no raw span text, class, or offsets |
| `PrivacyReceipt.detectors.gemma` | `version`, `invoked`, `schema_valid`, `approved_residual_spans` with the same string-reference shape |
| `PrivacyReceipt.outbound` | `scan_status`, `allowed_field_paths`, `raw_text_field_count` |
| `PrivacyReceipt.signature_ref` | Exactly `key_id`, `algorithm`, `signature`; all are non-empty strings and no secret key material is permitted; the detached signature covers the unsigned receipt body and the final artifact hash covers the signature reference |
| `CloudBoundPayload` | Non-artifact intake object. Version `1.0.0` requires variant fields `gene`, `hgvs_c`, `hgvs_p`, `assembly`. Version `1.1.0` retains `gene`, `hgvs_c`, and `assembly` but permits `hgvs_p` to be absent when the hash-verified source has no protein consequence (for example an intronic splice variant); it never fabricates a protein value. Both versions require exact top-level fields `payload_kind`, `payload_version`, `case_token`, `tenant_id`, `region`, `data_mode`, `variant`, allow optional `deidentified_summary`, and reject unknown fields. |
| `WatchCase.last_verified_scan` | `run_id`, `completed_at`; both null together when no verified scan exists |
| `WatchCase.pending_observation_hashes` | Sorted unique array of unverified observation hashes; empty is valid only after an explicit verified transition cleared the backlog |
| `WatchCase.attention_marker` | Null or exactly `reason_codes`, `first_seen_at`, `last_seen_at`, `related_run_ids`, `operator_action_required`; null only when no unresolved attention exists |
| `ScanRun.budget_snapshot` | `delegation_depth`, `specialist_invocations`, `model_calls_per_role`, `schema_repairs`, `agent_retries`, `connector_retries`, `repeated_state_limit`, `wall_time_seconds`, `step_deadlines`, `token_ceilings` |
| `RegistryResolutionReceipt.bindings[]` | `capability`, `agent_id`, `role`, `revision`, `manifest_digest`, `binding_id`, `region`, `validation_status` |
| `RegistryResolutionReceipt.resolution_mode` | Closed enum: `REGISTRY`, `MANUAL_SERVICE`, `PINNED_FALLBACK` |
| `CandidateDeltaReceipt.new_observation_hashes` | Sorted unique array; non-empty when `candidate_delta_state = PRESENT`, empty only when complete comparison proves `ABSENT`, and retained with failure reasons when determination is `UNKNOWN` |
| `CandidateDeltaReceipt.authority` | Produced only by the deterministic Evidence Normalizer; agent materiality proposals cannot create or modify it |
| `EvidenceDelta.comparison` | `classification_changed`, `classification_source_refs`; missing comparison evidence is not false |
| `CitationAuditReceipt.claim_verdicts[]` | `claim_id`, `verdict`, `reason_codes`, `refetched_source`; source contains `identifier`, `title`, `locator`, `content_hash`, and is null if and only if verdict is `UNAVAILABLE` |
| `CitationAuditReceipt.audit_status` | Closed enum: `COMPLETE`, `INCOMPLETE`; claim verdicts use `VERIFIED`, `MISMATCH`, `UNAVAILABLE` |
| `ReplayStage` | Closed replay cursor enum: `stage-0`, `stage-1`, `stage-2`; it controls deterministic source visibility and carries no outcome authority |
| `DataModeReceipt.mode_set` | Sorted unique non-empty array containing only `SYNTHETIC`, `CAPTURED_REPLAY`, `LIVE_PUBLIC`, or `MOCK` from the transitive input closure |
| `DataModeReceipt.declared_composition` | `SYNTHETIC_ONLY`, `CAPTURED_REPLAY_ONLY`, `LIVE_PUBLIC_ONLY`, `MOCK_ONLY`, or `SYNTHETIC_WITH_CAPTURED_REPLAY`; exact deterministic projection from `mode_set` |
| `FailureReceipt.details` | Registered failure-code-specific object; `loop_detected` permits only `hop_count` and `repeated_state_hash` |
| `DeploymentReceipt.runtime` | `service`, `revision`, `region`, `resource_name`, `read_back_at` |
| `CohortDayManifest.execution_history[]` | `3.3.0`: monotone history retaining exact legacy rows and adding completion-time ramp rows. `executed_at` records durable admission/write completion; per-run failures are carried by `run_outcomes`, failure receipts, and the manifest envelope status rather than rewriting a completed history row. The current row names its exact source schema version. |
| `COMPRESSED_PREDICTION_PLAN_V2.cycles[].predecessor` | Plan schema `2.4.0`: external predecessors carry exact plan SHA-256, collection prefix, manifest ID/hash, and mode-receipt ID/hash; current-plan predecessors derive those identifiers from the current plan. Missing or drifted evidence permits only one immutable DENIED ramp receipt and no ScanRun/event/manifest/mode write. |
| `COMPRESSED_PREDICTION_PLAN_V2.cycles[]` deadlines | Each cycle separately declares the trigger-start window, `write_timeout_seconds`, `agent_timeout_seconds`, and authoritative `execution_timeout_seconds`. The immutable end-to-end deadline is `window_start + execution_timeout_seconds`; runtime and UI must evaluate against the manifest's bound deadline policy, never infer completion authority from `window_end`. |
| `COMPRESSED_PREDICTION_PLAN_V2.cycles[].write_path` | Executed c1/c2 are `EXTERNAL_IMMUTABLE`; c3-c6 all use `FIRESTORE_BATCH_V1`. The final `PORTFOLIO_REASSESSMENT` therefore follows the same write and exact-readback path as the three ramp first passes. |
| `CohortDayManifest.cases[]` | Exactly `case_id`, `data_mode`, `vcv`; `vcv` is null for `SYNTHETIC_ONLY` and required for `SYNTHETIC_WITH_CAPTURED_REPLAY` |
| `CohortDayManifest.vcv_anchors[]` | Exactly `vcv`, `capture_path`, `sha256`, `artifact_id`; every path is relative and every hash binds committed capture bytes |
| `CohortDayManifest.delta` | Exactly `selected_case_ids`, `excluded_case_ids`, `newly_created_run_ids`, `reused_run_ids`, `authoritative_run_ids`, `runs_predicted`, `prediction_match`; `runs_created` in history is the authoritative post-reconciliation count, not invocation-local writes |
| `CohortDayManifest.cumulative` | `3.0.0`: exactly compressed cycles completed/successful, compressed runs predicted/created, actual distinct execution dates, logical days covered, and historical incomplete attempts. Multiple accelerated cycles on one UTC date never become a multi-day recurrence claim. |
| `CohortHeadroomReceipt` | Receipt ID includes the canonical c1-c5 input snapshot hash. For each `3.3.0` predecessor the snapshot and receipt inputs bind the exact `BatchExecutionReceipt` ID/hash; the gate replays that receipt against current pointers, immutable ScanRun artifacts, manifest run partitions, and write metrics. Every lifecycle row must also satisfy the closed `(from_state, event_code) -> to_state` table, sequence, pointer, lease-epoch, and exactly-one-`RUN_CREATED` invariants. Identical evidence reuses exact bytes; changed evidence creates another immutable receipt. Only a persisted exact PASS wire for the freshly recomputed snapshot may authorize c6, and the c6 manifest must name it in `headroom_receipt_id` and `input_artifact_ids`; DENIED writes no c6 ScanRun, event, manifest, or mode receipt. |
| `CohortRampGateReceipt` | Receipt ID includes the canonical predecessor snapshot hash. c3 verifies the exact externally executed c2 manifest/mode pair; c4-c6 additionally require predecessor `newly_created == predicted`, `reused == 0`, live persistence, and end-to-end write plus exact-readback at no more than 2000 ms per selected case. DENIED is the sole permitted target-ledger write. |
| `CohortDayManifest.write_metrics` | Exact scope `CASE_WRITE_AND_EXACT_READBACK`; ledger-operation invocation counts, committed case documents, start/completion timestamps, worker/read-back/total milliseconds, and derived per-case milliseconds. These are not represented as raw SDK RPC counts. |
| `CohortDayManifest.parity` | Expected/actual newly-created and reused run counts plus separate `epoch_parity_match` and `fresh_write_parity_match`. Exact current-epoch recovery proves set parity but proves fresh-write parity only when the durable `BatchExecutionReceipt` recorded the complete first attempt as `MEASURED`; partial pre-receipt recovery is `NOT_EVALUATED`, produces an `INCOMPLETE` manifest, and cannot gate a successor. |
| `CohortDayManifest.deadline_policy` | Exact paths include `trigger_started_at`, `trigger_window_end`, write/agent timeout and completion fields, and `authoritative_end_to_end_deadline`. The UI binding path is `$.deadline_policy.authoritative_end_to_end_deadline`; it must not compare completion to `window_end`. |
| `PrivacyReceipt` `1.1.0` execution fields | `execution_locus` is a closed locus such as `LAB_LOCAL` or `CLOUD_ISOLATED`; `transport_class` and `endpoint_class` state how the model was reached; `model_id` names the model and `model_revision` is `sha256:<64 hex>`. Missing externally supplied receipts fail closed; no synthetic fallback may conceal them. |
| FULL_AUDIT PrivacyReceipt execution path | Admission accepts exactly `LAB_LOCAL + LOCAL_PROCESS + OLLAMA_LOCAL`, `LAB_LOCAL + PRIVATE_SERVICE + OLLAMA_CLOUD_RUN`, or `LAB_LOCAL + PRIVATE_SERVICE + OLLAMA_VERTEX_ENDPOINT`. The Vertex custom-container path is primary and the Cloud Run path remains an approved fallback. Mixed triples and unlisted endpoints fail closed; `gemma.invoked=true` and `gemma.schema_valid=true` remain mandatory. |
| External FULL_AUDIT preparation sources | Bundle wire `2.2.0` binds both the external receipt file SHA-256 plus verifier-key fingerprint and the per-case laboratory-note file SHA-256, note schema, and note version. The note source contains exactly the 462 unique cases; repeated evaluation epochs reuse the same case-bound note. Its five VCV rows carry the exact case-to-VCV binding and a fully resolved source-derived variant; unresolved inheritance and any fabricated or mismatched VCV fail closed. Every receipt signature and exact cloud-payload hash is verified before preparation. Legacy wires `2.0.0` and `2.1.0` remain readable but cannot start `FULL_AUDIT_V1`; the fixed `_lab_note` path is legacy-only. Secret key material and raw note text are never copied into the preparation bundle. |
| `AgentExecutionReceipt.turns[]` | Ordered turn index, prompt/candidate/thought/total token counts, finish reason, function-call presence, and wall-clock latency. Prompt text and chain-of-thought are forbidden. STARTED and terminal receipts are separate immutable artifacts linked by `started_receipt_id`. |
| `AgentExecutionReceipt.tool_records[]` | Exact `tool_id`, `call_id`, `response_id`, observed `adk_invocation_id`, deterministic `request_id`, and `authorization_receipt_id`. The request ID is recomputed from run, role, attempt, role-execution invocation, observed ADK invocation, function-call ID, and tool ID. A role rejects a changed ADK invocation before reservation or backend dispatch. Every terminal COMPLETED receipt binds these authorization receipts as immutable inputs; the Ledger atomically verifies that exact causal identity, same-run role/tool identity, and `ALLOWED` before advancing state. |
| FULL-audit citation refetch | A material claim may be VERIFIED only from a run-scoped claim-to-cited-source binding followed by the fixed PubMed fetcher and deterministic `RefetchAdapter`. Missing binding or fetch result is `UNAVAILABLE` and makes the audit INCOMPLETE; model-proposed metadata never creates a verdict. |
| `CohortDayManifest.agent_execution_summary` | Declares `FULL_AUDIT_V1`, runtime `IN_PROCESS_ADK_CLOUD_RUN`, concurrency, exact model/endpoint class, COMPLETE/INCOMPLETE/NOT_EVALUATED/HALTED counts, invocation/token/latency/429 telemetry, projected/reserved cost, hash-bound pricing policy, and `actual_billed_cost_state=NOT_VERIFIED`. It never claims that Agent Engine executed the cohort. |
| `CohortDayManifest.run_outcomes[]` | One case/run/epoch row with terminal state, three-valued audit status, CitationAudit and PolicyDecision bindings, deterministic policy outcome/reasons, typed technical failures, all paired STARTED/terminal AgentExecution receipt IDs, and elapsed time. A completed three-role run has at least six IDs; one crash-resume retry per role may increase the even count to twelve. HALTED requires null policy fields and a typed failure receipt. |
| `CohortDayManifest.image_digest` | Required lowercase OCI digest shape `sha256:<64 hex>`; identifies the immutable image that produced the manifest |
| `CohortDayFailureReceipt` | One deterministic ID per missing cohort date; first detection timestamp is immutable, retry reuses the persisted wire, `runs_created=0` is permitted only after the prior namespace has zero ScanRuns and zero `RUN_CREATED` events, and partial state fails closed. |
| `CohortHistoryReceipt` | Deterministic wire derived only from the exact committed `first.json` bytes; the raw SHA-256, Git blob OID, source commit/tree, all successful atomic checks, direct exit 0, and one ScanRun plus one RUN_CREATED read-back must agree before downstream writes |

Fields marked nullable in the eventual machine schema still remain present. `null` means not applicable or not yet available only when the contract explicitly permits it; it never means passed.

## Policy input fact schema

`PolicyDecision.input_facts` is the only policy payload. It contains closed evaluated states computed from validated authoritative artifacts, never agent prose or memory state.

`FactState` fields allow only `PASS`, `FAIL`, or `NOT_EVALUATED`:

- `privacy_accepted`
- `registry_resolution_valid`
- `route_valid`
- `tool_authorization_complete`
- `source_retrieval_complete`
- `source_schema_valid`
- `data_mode_valid`
- `snapshot_integrity_valid`
- `assessment_valid`
- `citation_audit_complete`
- `all_material_claims_verified`
- `counter_evidence_complete`

`PresenceState` fields allow only `PRESENT`, `ABSENT`, or `UNKNOWN`:

- `candidate_delta_state`
- `unresolved_conflict_state`
- `budget_or_loop_failure_state`
- `existing_open_task_state`

`candidate_delta_state` is projected only from a valid `CandidateDeltaReceipt`. `EvidenceDelta.materiality_proposal` is non-authoritative and cannot change it. Memory receipts and memory-conflict state are forbidden policy fields.

## Unknown-field and compatibility behavior

| Input condition | Required result |
|---|---|
| Unknown top-level or nested field | Reject with `contract_unknown_field` |
| Unsupported major version | Reject with `contract_major_unsupported` |
| Unsupported newer minor version | Reject unless exact backward compatibility is registered |
| Missing required field | Reject with `contract_required_field_missing` |
| Empty value where completeness is required | Mark `INCOMPLETE` or reject, never infer clean |
| Invalid hash or input reference | Reject with `artifact_integrity_failed` |
| Producer identity not authorized for contract | Reject with `producer_not_authorized` |
| Input modes form an allowed registered composition | Accept and record the exact sorted `mode_set` and `declared_composition` |
| Input modes form an unregistered composition, including `MOCK` plus product evidence or `LIVE_PUBLIC` inside a captured replay timeline | Reject with `data_mode_conflict` |

## Valid examples

```json
{
  "schema_name": "ToolAuthorizationReceipt",
  "schema_version": "1.0.0",
  "artifact_id": "cbb18422-a25c-4dc1-bc41-0dcb45e025a5",
  "case_id": "2bddc33c-25a8-4578-93d3-e286837e718d",
  "run_id": "f0fd6a3e-5897-4894-b5f6-d3ec18be1823",
  "producer": {"component": "tool-authorizer", "version": "0.1.0", "identity": "gateway-authorizer"},
  "created_at": "2026-08-16T09:00:00Z",
  "input_artifact_ids": [],
  "content_hash": "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",
  "data_mode": "SYNTHETIC",
  "status": "REJECTED",
  "warnings": [],
  "extensions": {},
  "agent_role": "EVIDENCE_WATCHER",
  "tool_id": "unregistered-browser",
  "requested_action": "open_arbitrary_url",
  "decision": "DENIED",
  "policy_version": "1.0.1",
  "reason_codes": ["tool_not_allowlisted"],
  "invocation_id": "fdc016c8-21c3-4e59-95de-5ca8710796e1"
}
```

```json
{
  "schema_name": "PolicyDecision",
  "schema_version": "2.0.0",
  "artifact_id": "0a651403-8226-4072-9240-344542b0c5fb",
  "case_id": "2bddc33c-25a8-4578-93d3-e286837e718d",
  "run_id": "f0fd6a3e-5897-4894-b5f6-d3ec18be1823",
  "producer": {"component": "policy-gate", "version": "1.0.1", "identity": "policy-service"},
  "created_at": "2026-08-16T09:01:00Z",
  "input_artifact_ids": ["cbb18422-a25c-4dc1-bc41-0dcb45e025a5"],
  "content_hash": "bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb",
  "data_mode": "SYNTHETIC",
  "status": "VALID",
  "warnings": [],
  "extensions": {},
  "policy_version": "1.0.1",
  "input_facts": {
    "privacy_accepted": "PASS",
    "registry_resolution_valid": "PASS",
    "route_valid": "PASS",
    "tool_authorization_complete": "FAIL",
    "source_retrieval_complete": "PASS",
    "source_schema_valid": "PASS",
    "data_mode_valid": "PASS",
    "snapshot_integrity_valid": "PASS",
    "candidate_delta_state": "PRESENT",
    "assessment_valid": "PASS",
    "citation_audit_complete": "NOT_EVALUATED",
    "all_material_claims_verified": "NOT_EVALUATED",
    "counter_evidence_complete": "NOT_EVALUATED",
    "unresolved_conflict_state": "PRESENT",
    "budget_or_loop_failure_state": "ABSENT",
    "existing_open_task_state": "UNKNOWN"
  },
  "outcome": "ABSTAIN",
  "reason_codes": ["citation_audit_not_evaluated", "counter_evidence_not_evaluated", "material_claims_not_evaluated", "tool_authorization_incomplete", "unresolved_evidence_conflict"],
  "missing_prerequisites": ["all_material_claims_verified", "citation_audit_complete", "counter_evidence_complete", "tool_authorization_complete"],
  "review_trigger": false,
  "existing_task_id": null
}
```

The literal UUIDs, timestamps, and hashes above are documentation fixtures only. Executable fixtures must calculate hashes and times and must never use these values as result presets.

## Implementation acceptance

RCL-202 design is complete only at the document level. RCL-302 must implement machine-readable schemas, positive and negative fixtures, canonical hash tests, producer-authorization tests, and unknown-field mutation tests before any runtime claim is verified.
