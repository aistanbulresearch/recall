# RCL-205 Replay Protocol 1.0.1 Verification

- Date: 2026-08-17
- Scope: ADR-0008 decisions 7 and 8, audit findings F-07 and F-08
- Result: PASS at frozen-source-package and offline-verifier level
- Product replay execution: not started
- External follow-up audit: first re-review failed at remote commit `05ff0b59`; four findings are remediated locally and require a second review before publish

## Outcome

Protocol 1.0.1 replaces dynamic-response reference hashes with exact repository bytes under `artifacts/evidence/rcl-205/`. The manifest binds ten permitted captures by relative path, exact byte count, SHA-256, raw-versus-normalized hash role, retrieval timestamp, semantic anchor, media type, transformation, per-source rights profile, limitation, attribution, and redistribution boundary.

The verifier requires no network access and rejects absolute or parent capture roots before capture processing, lexical path escape, junction escape, missing files, byte drift, hash-role drift, malformed provenance or rights binding, disallowed source hosts, source-derived chronology mismatches, incorrect ClinVar classification/citation semantics, incorrect publication-to-GEO linkage, a stored-versus-actual excerpt word-count mismatch, and an incorrect or non-unique exact XLSX row.

## Frozen capture inventory

| Class | Files | Bytes | Boundary |
|---|---:|---:|---|
| ClinVar archived printable records | 5 | 808,009 | Attributed public NCBI record captures; accession.version remains the semantic anchor |
| PubMed ESummary metadata | 2 | 5,399 | Bibliographic metadata only; no abstract or full text |
| GEO metadata and exact result workbook | 2 | 585,635 | Public NCBI GEO capture with accession and as-captured row limitation |
| Nature Data availability linkage | 1 | 826 | Fifteen-word excerpt only; no article, abstract, figure, table, or supplement |
| Total | 10 | 1,400,869 | All paths and hashes are frozen in manifest 1.0.1 |

## Chronology and linkage correction

The offline package independently binds these distinct facts:

| Fact | Frozen value | Captured authority |
|---|---|---|
| GEO submission date | 2023-11-22 | `GSE248438.brief.soft.txt` |
| GEO public date | 2024-09-27 | `GSE248438.brief.soft.txt` |
| Qualifying publication date | 2025-01-08 | PMID 39779848 ESummary `epubdate` |
| Later evaluator date | 2026-02-03 | Frozen ClinVar submission chronology |
| ClinVar v4 update | 2026-04-13 | VCV002895953.4 semantic record |
| Later public ClinVar appearance | 2026-04-25 | VCV002895953.5 semantic record |
| GEO last update as captured | 2026-07-29 | `GSE248438.brief.soft.txt` |
| GEO current linked PMID as captured | 41957374 | `GSE248438.brief.soft.txt` |
| Qualifying Nature PMID | 39779848 | PubMed ESummary plus DOI |
| Paper-to-dataset link | DOI 10.1038/s41586-024-08349-1 names GSE248438 in Data availability | Minimal Nature linkage capture |

The Auditor must not require GEO's current linked PMID to equal the earlier qualifying Nature PMID. The exact workbook row is proven only in the XLSX bytes captured at `2026-08-16T23:18:35Z`; it is not represented as proof that the row-level file existed on the GEO public date.

## Executed verification

Command:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\evidence\verify-rcl-205-captures.ps1
```

Observed result:

```text
status=PASS
manifest_version=1.0.1
verified_captures=10
verified_bytes=1400869
chronology_checks=7
source_semantic_checks=12
rights_metadata_checks=11
live_connector_spec_checks=1
exact_xlsx_rows=1
live_public_sources=1
network_calls=0
```

Mutation and path-boundary test:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\scripts\evidence\test-rcl-205-captures.ps1
```

Observed result:

```text
status=PASS
clean_copy_verified=true
verified_captures=10
mutated_byte_rejected=true
path_traversal_rejected=true
absolute_capture_root_rejected=true
parent_capture_root_rejected=true
clinvar_semantic_mutation_rejected=true
nature_word_count_mutation_rejected=true
rights_profile_mutation_rejected=true
live_rights_profile_mutation_rejected=true
live_runtime_rule_mutation_rejected=true
live_duplicate_rejected=true
hash_role_mutation_rejected=true
junction_escape_rejected=true
network_calls=0
```

The fault harness creates independent package copies in a validated temporary directory. It requires typed rejection for a flipped source byte, relative capture traversal, an absolute capture root, a parent capture root, a hash-rebound ClinVar citation mutation, a hash-rebound Nature excerpt word-count mutation, invalid captured/live rights-profile bindings, an invalid live runtime rule, a duplicate live source ID, an invalid raw/normalized hash role, and an in-root junction that targets an out-of-root directory. Root rejection must occur without downstream hash or missing-file errors. The temporary tree is removed only after its absolute path is confirmed inside the system temporary directory.

`chronology_checks`, `source_semantic_checks`, `rights_metadata_checks`, `live_connector_spec_checks`, and `exact_xlsx_rows` derive only from successful named assertions. The harness asserts the complete semantic, rights, and live-spec ID sets. The 391-day and 472-day intervals are calculated from dates parsed from frozen PubMed and ClinVar captures, not from a second copy of manifest dates. The Ambry submission citation check requires PMID `39779848` and rejects PMID `39779857`; Huang et al. remains separately labeled corroborating literature.

The XLSX reader found exactly one matching row, row 244 of Sheet 1:

```text
15.2 | Non-synonymous | G:C | G2508R | NM_000059.4(BRCA2):c.7522G>C | NC_000013.11:g.32356514G>C | Pathogenic Strong | -1.44817784674576 | 0.99950033514877201
```

Every displayed source value remains a captured upstream value. It is not a Recall classification or decision threshold.

## Failure history retained

- The first test-file patch failed because its parent directory did not yet exist.
- Direct script execution was blocked by the machine's execution policy; all evidence commands use process-scoped `-ExecutionPolicy Bypass` and do not change machine policy.
- The first verifier run evaluated `$PSScriptRoot` too early in a parameter default and was corrected for Windows PowerShell 5.1.
- The first mutation run reported a downstream JSON parser error after already detecting hash drift; corrupted captures are now excluded from semantic parsing so the verifier emits the typed integrity failure.
- The first XLSX integration used a missing dynamic XML property under StrictMode; it now uses explicit attribute access.
- One compressed review command failed before execution because of malformed `foreach` spacing and was rerun readably.
- The first expanded test invocation was blocked by the machine execution policy; it was rerun with process-scoped `-ExecutionPolicy Bypass`.
- The first production-tree reparse defense treated OneDrive cloud-placeholder attributes as link escapes. It now rejects only target-bearing link/junction reparse points while preserving the junction-escape fault test.

The canonical error IDs and resolutions are retained in `docs/project/ERROR_LOG.md`.

## Boundaries

- F-07 and F-08 pass locally at source-package level.
- RCL-205 does not prove that Recall detected the evidence, invoked an agent, emitted a PolicyDecision, created a task, or achieved the 472-day case-specific lead time operationally.
- RCL-503, RCL-506, RCL-801, merge, a second publish, external auditor re-review, and Phase 3 remain governed by their separate gates.
- The current live ClinVar connector is a separate `LIVE_PUBLIC` source with no frozen expected hash.
- Git index reproducibility is protected by `.gitattributes`: the exact replay evidence tree is `binary -eol`. Before commit, all ten working files and all ten staged blobs matched their manifest SHA256 values. Clean-clone verification remains required after commit.
- Cursor added another bot comment immediately after the owner-only `05ff0b59` push. The exact comment was removed and remote surfaces were reread clean, but this proves the integration was not disabled. No remediation push is allowed until owner-side disablement is independently confirmed by a clean post-push actor scan.
